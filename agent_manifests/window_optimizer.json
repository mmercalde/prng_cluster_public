{
  "agent_name": "window_optimizer_agent",
  "description": "Step 1: Bayesian window optimization using Optuna TPE. Runs real forward/reverse sieves across 26 GPUs to find optimal window_size, offset, and skip_range parameters.",
  "pipeline_step": 1,
  "version": "1.7.0",
  "required_inputs": [
    "daily3.json"
  ],
  "primary_output": "optimal_window_config.json",
  "feature_registry": "config_manifests/feature_registry.json",
  "inputs": [
    "lottery_file",
    "seed_count",
    "prng_type"
  ],
  "outputs": [
    "bidirectional_survivors.json",
    "optimal_window_config.json"
  ],
  "actions": [
    {
      "type": "run_script",
      "script": "window_optimizer.py",
      "args_map": {
        "lottery-file": "lottery_file",
        "trials": "window_trials",
        "max-seeds": "seed_count",
        "prng-type": "prng_type",
        "strategy": "search_strategy",
        "forward-threshold": "forward_threshold",
        "reverse-threshold": "reverse_threshold",
        "test-both-modes": "test_both_modes",
        "enable-pruning": "enable_pruning",
        "n-parallel": "n_parallel",
        "resume-study": "resume_study",
        "study-name": "study_name",
        "use-persistent-workers": "use_persistent_workers",
        "worker-pool-size": "worker_pool_size",
        "seed-cap-nvidia": "seed_cap_nvidia",
        "seed-cap-amd": "seed_cap_amd"
      },
      "distributed": true,
      "timeout_minutes": 240
    }
  ],
  "follow_up_agents": [
    "scorer_meta_agent"
  ],
  "success_condition": [
    "optimal_window_config.json",
    "bidirectional_survivors.json"
  ],
  "retry": 2,
  "parameter_bounds": {
    "window_size": {
      "type": "int",
      "min": 2,
      "max": 50,
      "default": 2,
      "description": "Size of the temporal window (number of draws) for PRNG matching.",
      "optimized_by": "Optuna TPE",
      "effect": "Short-term temporal regime confirmed (167-trial run S138). TRSE Rule A caps at 32 when short_persistence active. Config ceiling 50 provides buffer. W2 dominant empirically."
    },
    "offset": {
      "type": "int",
      "min": 0,
      "max": 2000,
      "default": 0,
      "description": "Time offset from current draw position.",
      "optimized_by": "Optuna TPE",
      "effect": "Non-zero offset may capture periodic PRNG reseeding patterns."
    },
    "skip_min": {
      "type": "int",
      "min": 0,
      "max": 50,
      "default": 0,
      "description": "Minimum skip value for variable skip PRNG patterns.",
      "optimized_by": "Optuna TPE",
      "effect": "Controls lower bound of skip range search."
    },
    "skip_max": {
      "type": "int",
      "min": 20,
      "max": 500,
      "default": 100,
      "description": "Maximum skip value for variable skip PRNG patterns.",
      "optimized_by": "Optuna TPE",
      "effect": "Controls upper bound of skip range search. Higher = more flexibility."
    },
    "forward_threshold": {
      "type": "float",
      "default": 0.25,
      "description": "Forward sieve match threshold. Higher = stricter filtering.",
      "optimized_by": "Optuna TPE",
      "effect": "Controls false positive rate in forward sieve.",
      "_note": "Bounds are INFORMATIONAL ONLY. Runtime bounds defined in distributed_config.json",
      "_bounds_reference": "[0.15, 0.60] - see distributed_config.json"
    },
    "reverse_threshold": {
      "type": "float",
      "default": 0.25,
      "description": "Reverse sieve match threshold. Controls historical consistency.",
      "optimized_by": "Optuna TPE",
      "effect": "Controls historical consistency check.",
      "_note": "Bounds are INFORMATIONAL ONLY. Runtime bounds defined in distributed_config.json",
      "_bounds_reference": "[0.15, 0.60] - see distributed_config.json"
    },
    "window_trials": {
      "type": "int",
      "min": 10,
      "max": 200,
      "default": 50,
      "description": "Number of Optuna trials for window parameter optimization.",
      "optimized_by": "Manual",
      "effect": "More trials = better exploration but longer runtime. 50-100 typical."
    },
    "seed_count": {
      "type": "int",
      "min": 1000000,
      "max": 5000000000,
      "default": 50000000,
      "description": "Number of candidate seeds to test per trial.",
      "optimized_by": "Manual",
      "effect": "More seeds = higher recall but O(n) slower. 10M-100M for testing, 1B+ for production."
    },
    "search_strategy": {
      "type": "choice",
      "choices": [
        "bayesian",
        "random",
        "grid",
        "evolutionary"
      ],
      "default": "bayesian",
      "description": "Optuna search strategy for parameter space exploration.",
      "optimized_by": "Fixed",
      "effect": "bayesian (TPE) is most sample-efficient. Use random for quick sanity checks."
    },
    "test_both_modes": {
      "type": "bool",
      "default": true,
      "description": "Test BOTH constant AND variable skip patterns (V2.0 feature).",
      "optimized_by": "Manual",
      "effect": "Doubles search space but discovers variable skip patterns."
    },
    "use_persistent_workers": {
      "type": "bool",
      "default": false,
      "description": "[S130] Use persistent GPU sieve workers (sieve_gpu_worker.py) instead of per-job subprocess launch. Eliminates per-job ROCm init overhead. Requires soak test validation before enabling in production.",
      "optimized_by": "Manual",
      "effect": "Expected 4-5x throughput gain on AMD rigs by keeping ROCm initialized across jobs."
    },
    "worker_pool_size": {
      "type": "int",
      "min": 1,
      "max": 8,
      "default": 8,
      "description": "[S130] Number of persistent worker processes to spawn per AMD rig (1 per GPU). Zeus always uses execute_local_job regardless of this setting.",
      "optimized_by": "Manual",
      "effect": "Set to 8 for full rig utilization. Reduce for debugging or partial-rig testing."
    },
    "seed_cap_nvidia": {
      "type": "int",
      "min": 100000,
      "max": 10000000,
      "default": 5000000,
      "description": "[S129B-A] Max seeds per job chunk for NVIDIA GPUs (RTX 3080 Ti). Measured hardware ceiling ~2.2M sps.",
      "optimized_by": "Manual",
      "effect": "Controls chunk sizing for Zeus GPUs. 5M = safe production value at ~2.2M sps measured throughput."
    },
    "seed_cap_amd": {
      "type": "int",
      "min": 100000,
      "max": 5000000,
      "default": 2000000,
      "description": "[S129B-A] Max seeds per job chunk for AMD GPUs (RX 6600). Measured hardware ceiling ~787k sps.",
      "optimized_by": "Manual",
      "effect": "Controls chunk sizing for rig GPUs. 2M = safe production value at ~788k sps measured throughput."
    }
  },
  "success_metrics": {
    "min_bidirectional_survivors": 1,
    "target_forward_survivors": 100,
    "target_reverse_survivors": 100,
    "max_false_positive_ratio": 0.1
  },
  "default_params": {
    "lottery_file": "daily3.json",
    "strategy": "bayesian",
    "max_seeds": 10000000,
    "prng_type": "java_lcg",
    "output": "optimal_window_config.json",
    "test_both_modes": true,
    "resume_study": false,
    "window_trials": 100,
    "study_name": "",
    "trse_context": "trse_context.json",
    "enable_pruning": false,
    "n_parallel": 1,
    "use_persistent_workers": false,
    "worker_pool_size": 8,
    "seed_cap_nvidia": 5000000,
    "seed_cap_amd": 2000000
  },
  "evaluation_params": {
    "confidence_formula": "bit_length",
    "confidence_base": 0.7,
    "confidence_divisor": 64.0,
    "confidence_cap": 0.95,
    "zero_survivors_confidence": 0.5
  },
  "evaluation_type": "file_exists",
  "disable_llm_parsing": true,
  "disable_heuristic_parsing": true,
  "retry_policy": "none",
  "args_map": {
    "study-name": "study_name"
  },
  "param_docs": {
    "study_name": {
      "type": "str",
      "default": "",
      "description": "Optuna study DB name to resume (e.g. 'window_opt_1772507547'). Empty = auto-select most recent incomplete study.",
      "optimized_by": "Manual"
    }
  }
}